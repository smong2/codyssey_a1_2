A-1. 폴더 및 파일 생성

     travel_planner/

     ├── travel_[planner.py](http://planner.py)

     ├── .env

     ├── .gitignore

     ├── requirements.txt

     └── results/           ← 자동 생성됨 (직접 만들 필요 없음)

A-2. 패키지 설치

     pip install openai requests python-dotenv

A-3. .env 파일에 API 키 작성

     OPENAI_API_KEY=...
     naver search api : .env 에 설정

     KAKAO_API_KEY=...

     ※ 실제 키 값은 절대 코드/README/결과물에 포함하지 않는다

A-4. .gitignore 설정

     .env

     results/

     **pycache**/
